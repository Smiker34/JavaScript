1)Код я нормальноне реализовал, поэтому попытаюсь объяснить задумку(не до конца уверен, насколько она правильная):
a.)Основная функция, которая будет отрабатывать столкновение со стеной, т.е. переносить змею. В неё передаётся переменная текующие координаты головы.
b.)Она возвращает функцию, которая в зависимости от направления при столкновении, высчитывает нужный столбец/колонку.
c.)Переданной основной функции переменной задаётся новое значение - координаты противоположенного конца высчитанного столбца/колонки.

Альтернативный вариант, который, кажется, не правильный:
a.)Основная функция, которая будет отрабатывать столкновение со стеной, т.е. переносить змею. В неё передаются текующие координаты головы.
b.)В неё вложена функция, которая в зависимости от направления при столкновении, высчитывает нужный столбец/колонку и возвращает координаты противоположенного конца.
c.)Переданные значения координат меняются на новые, путём отработки вложенной функции.
d.)Основная функция возвращает результат.

------------------------------------------------------------------------------------------

2a)Предполагаю, что undefined потому что при первой прогрузке скрипта, заранее создаётся глобальная переменная a, но не задаётся значение.
Поэтому условие не срабатывает из-за наличия переменной, и значение остаётся undefined.

2b)Наверное a is not defined, что функция a создайтся как значение b, но не как самостоятельная переменная.

2c)Саму функцию, по логике, схожей с первым пунктом, что переменная создаётся ещё при первой прогрузке, а затем добавляется значение.

2d)10, потому что функция просто задаёт значение по индексу. И элемент по индексу совпадает с выводимым.

2e)Либо срабатывает какая-нибудь логика, что при null присвается что-нибудь иное, например window, либо всё куда проще и просто null.
Возможно здесь есть какой-то подвох (думаю отметка * не просто так), и здесь правильны оба варианта, но зависит от какого-нибудь параметра.